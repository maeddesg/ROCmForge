Additionally, the server should support a POST request to '/upload' endpoint which accepts a file and saves it to the server's file system. The server should also log all incoming requests to a file named 'requests.log'.
To create a Rust HTTP server that listens on port 8080, responds to GET requests with a JSON object containing the current timestamp, and supports a POST request to '/upload' endpoint which accepts a file and saves it to the server's file system, you can use the `hyper` crate for HTTP handling and `tokio` for asynchronous operations. Additionally, you can use the `log` crate for
