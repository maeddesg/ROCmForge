
 Additionally, implement a POST endpoint that accepts a JSON payload with a "message" field and returns a JSON response with the received message and the current timestamp.
To create a Rust HTTP server that listens on port 8080 and responds to GET and POST requests, you can use the `hyper` crate. First, add the `hyper` crate to your `Cargo.toml` file:

```toml
[dependencies]
hyper = "0.14"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
```

Next, create a new Rust file
