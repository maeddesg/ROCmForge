Write a JavaScript function called groupBy that takes an array of objects and a key name, and returns an object where the keys are the unique values of that property and the values are arrays of matching objects.
