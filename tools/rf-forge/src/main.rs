// rf-forge — ROCmForge v1.0 offline GA tuning tool (Phase 1 stub).
//
// Real subcommands land in Phase 2 per architecture_v1.2.0-draft §4.7.

fn main() {
    println!("rf-forge v0.1.0 — ROCmForge GA Tuning Tool");
    println!(
        "Verfügbare Subcommands: tune-all, tune-kernels, tune-precision, \
         cache, validate, bench, export"
    );
    println!("Phase 2 — noch nicht implementiert.");
}
